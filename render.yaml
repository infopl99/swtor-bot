services:
  - type: worker  # Indique que c'est un Background Worker
    name: swtor-bot
    runtime: python
    repo: https://github.com/infopl99/swtor-bot
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
