services:
  - type: worker
    name: swtor-bot
    runtime: python
    repo: https://github.com/infopl99/swtor-bot
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
